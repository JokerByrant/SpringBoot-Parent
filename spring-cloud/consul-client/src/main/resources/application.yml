server:
  port: 7206

spring:
  application:
    name: consul-client
  cloud:
    # 将服务注册到consul
    consul:
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}