server:
  port: 7901

spring:
  application:
    name: config-center
  cloud:
    config:
      server:
        native:
          searchLocations: classpath:/config
  profiles:
    active: native

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
